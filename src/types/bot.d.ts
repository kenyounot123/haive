export interface Bot {
    // id: string;
    name: string;
    description: string;
    likes: number;
    prompt: string;
    // icon?: string; // b64 encoded image
    // model?: string; // bot model

    // created_at: Date;
    // updated_at: Date;
}